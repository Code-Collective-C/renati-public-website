# renati-public-website

This site uses [Eleventy](https://www.11ty.io/), a static site generator.

Install Eleventy like this: `npm install -g @11ty/eleventy`.
Then run it like this: `eleventy --serve`.
